import { describe, expect, it } from 'vitest';
import { ProductDetailPageComponent } from './product-detail-page';

describe('ProductDetailPageComponent', () => {
  it('is defined', () => {
    expect(ProductDetailPageComponent).toBeDefined();
  });
});
